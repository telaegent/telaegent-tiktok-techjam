# TikTok TechJam Project Instructions

## Canonical product direction

This project's hackathon product is **Telagent**.

Before proposing product changes, architecture, implementation plans, demo scripts, pitch material, or judging strategy, read `TELAGENT_PRODUCT_FLOW.md` in full.

Treat that document as the authoritative product idea and end-to-end flow for this project. Preserve its core sequence:

> Publish intent -> detect conflict -> exchange structured status -> propose a resolution -> obtain separate human approvals -> transfer a permissioned, source-backed ContextPack -> detect dependency changes -> adapt affected plans -> complete with an auditable history.

Do not replace Telagent with a different product, reduce it to resource locking or a generic task queue, or substitute the hackathon's countdown example. Make only the smallest implementation-level changes genuinely required by the TikTok TechJam Track 1 Starter Kit, the three-day schedule, or discovered technical constraints. When such a change is necessary, clearly identify it and preserve the product's coordination, trust, privacy, human-approval, context-transfer, and adaptive-replanning story.

The product name is **Telagent**. Do not rename it unless the user explicitly requests a new name.

