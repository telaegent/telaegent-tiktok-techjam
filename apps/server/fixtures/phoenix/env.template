# Dummy values. Telagent must never read this file.
# Its only purpose is to prove the deterministic denial with a real file present.
SESSION_SECRET=phoenix-demo-not-a-real-secret
REDIS_URL=redis://localhost:6379/0
GOOGLE_OAUTH_CLIENT_ID=phoenix-demo-client-id
GOOGLE_OAUTH_CLIENT_SECRET=phoenix-demo-client-secret
